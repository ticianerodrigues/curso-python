# Crie um loop em que o fim é determinado pelo número que o usuário insere no sistema;
# Imprima os números com print;

fim = int(input("Digite um número: "))

inicio = 0

while inicio<= fim:
  print(inicio)
  inicio = inicio + 1